<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.huawei.hicloud.drive">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission
        android:name="android.permission.WRITE_MEDIA_STORAGE"
        tools:ignore="ProtectedPermissions" />

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- for HMS SDK upgrade -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />


    <application
        android:name="com.huawei.cloud.drive.MainApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.Holo.Light"
        tools:replace="android:allowBackup"
        android:uiOptions="splitActionBarWhenNarrow">

        <!-- Access HMSSDK need to register AppID parameters. Value "${APPID}" is replaced
            with the actual application APPID,derived from the Developer affiliate website
            application. Format android:value= "appid=xxxxxx" -->
        <meta-data
            android:name="com.huawei.hms.client.appid"
            android:value="appid=${APPID}" />

        <!--
            Subscription Capability Interface
            The value of value "x.x.x" uses the version number of the integrated HSSDK, such as hmssdk-base:3.0.0.300 above.，
            Here it corresponds android:value="3.0.0" -->
        <meta-data
            android:name="com.huawei.hms.version"
            android:value="4.0.0" />

        <meta-data
            android:name="hwc-theme"
            android:value="androidhwext:style/Theme.Emui.NoActionBar" />

        <!-- HMS upgrade provider -->
        <!-- com.huawei.cloud.drive.hms.update.provider -->
        <!-- com.huawei.drive.hidisk.hms.update.provider grey-->
        <provider
            android:name="com.huawei.hms.update.provider.UpdateProvider"
            android:authorities="com.huawei.cloud.drive.hms.update.provider"
            android:exported="false"
            android:grantUriPermissions="true" />

        <activity android:name="com.huawei.cloud.drive.MainActivity"  android:screenOrientation="portrait">
        </activity>
        <activity
            android:name="com.huawei.cloud.drive.SplashActivity"
            android:exported="true"
            android:screenOrientation="portrait">
            <meta-data
                android:name="hwc-theme"
                android:value="androidhwext:style/Theme.Emui.NoActionBar" />

            <intent-filter>
                <category android:name="android.intent.category.LAUNCHER" />
                <action android:name="android.intent.action.MAIN" />
            </intent-filter>
        </activity>

    </application>
</manifest>